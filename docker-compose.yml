services:
  homeassistant:
    image: homeassistant/home-assistant:stable
    container_name: hass-dev
    ports:
      - "8123:8123"
    volumes:
      - ./config:/config
      - ./buienradar-rain-card.js:/config/www/buienradar-rain-card.js:ro
    restart: unless-stopped
